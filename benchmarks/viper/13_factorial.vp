# Factorial Benchmark - Viper Implementation
# Recursive Factorial

def fact(n):
    if n <= 1:
        return 1
    return n * fact(n - 1)

def main():
    n = 15
    
    result = fact(n)
    print(result)

main()