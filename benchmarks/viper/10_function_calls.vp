# Function Call Overhead Benchmark - Viper Implementation

def mix(x, y):
    return ((x * 3) + (y * 5) - (x % 7) + (y % 11)) % 1000003

def main():
    n = 1500000
    acc = 0
    i = 0

    while i < n:
        acc = acc + mix(i, acc + i)
        i = i + 1

    print(acc)

main()
