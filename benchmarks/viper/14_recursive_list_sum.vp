# Recursive List Sum Benchmark - Viper Implementation
# Recursive sum of a list

def sum_list(lst, idx):
    if idx >= len(lst):
        return 0
    return lst[idx] + sum_list(lst, idx + 1)

def main():
    lst = list(range(1, 1001))  # 1 to 1000
    
    result = sum_list(lst, 0)
    print(result)

main()