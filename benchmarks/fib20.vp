# Test fibonacci with smaller value for quick verification
def fib(n: int) -> int:
    if n <= 1:
        return n
    return fib(n - 1) + fib(n - 2)

def main():
    n = 20
    result = fib(n)
    print("fib(" + str(n) + ") = " + str(result))
