def add_one(x: int) -> int:
    return x + 1

def double(x: int) -> int:
    return x * 2

def main():
    result = 5 |> add_one |> double
    print(result)

    result2 = 1 |> add_one |> double |> add_one
    print(result2)
