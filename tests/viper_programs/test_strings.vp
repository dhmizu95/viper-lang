def main() -> i64:
    s = "Hello World"
    up = s.upper()
    print(up)
    
    lo = s.lower()
    print(lo)
    
    str2 = "apple,banana,cherry"
    parts = str2.split(",")
    print("split OK")
    
    tgt = "World"
    new_tgt = "Viper"
    rep = s.replace(tgt, new_tgt)
    print(rep)
    
    return 0
