# Simple Result test with str error
def divide(a: i64, b: i64) -> Result[i64, str]:
    if b == 0:
        return Err("Division by zero")
    return Ok(a / b)

def test():
    r1: Result[i64, str] = divide(10, 2)
    r2: Result[i64, str] = divide(10, 0)
    print("Result test passed!")

test()
