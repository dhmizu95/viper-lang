# Test union types - basic parsing and type checking

# Union type with multiple variants
def handle_mixed(value: i64 | str | bool) -> str:
    return "handled"

# Variable with union type annotation
x: int | str = 42
y: int | str = "hello"

# Test type compatibility
def test_compatibility():
    a: int | str = 10
    b: int | str = "test"
    print(a)
    print(b)

# Nested union in container
def process_list(items: list[int | str]) -> int:
    return len(items)

# Union with complex types
def complex_union(data: dict[str, int | str] | list[int | str]) -> None:
    pass

print("Union type tests passed")
