def add_one(x: i64) -> i64:
    return x + 1

def double(x: i64) -> i64:
    return x * 2

def main():
    # Try with parentheses to force evaluation order
    result = (5 |> add_one) |> double
    print(result)
