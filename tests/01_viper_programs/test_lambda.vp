def apply(f: Callable, x: int) -> int:
    return f(x)

def main() -> int:
    f = lambda x: x * 2
    print(f(21))
    print("\n")
    
    res = apply(f, 5)
    print(res)
    print("\n")
    
    return 0
