# Test: Mutable variables (all variables are mutable by default)
def main():
    # All variables are mutable by default
    x = 10
    print(x)

    # Mutable variable
    counter = 0
    counter = counter + 1
    counter = counter + 5
    print(counter)

    # None value
    nothing = None
    print(0)

