def test_list_methods():
    lst = [1, 2, 3]
    lst.append(4)
    lst.extend([5, 6])
    lst.insert(1, 10)
    lst.pop()
    
    comp = [x*2 for x in range(3)]
    slice1 = lst[1:3]
    concat = lst + [7, 8]
    has_two = 2 in lst
    
    lst.clear()
    
    print("list methods ok")

test_list_methods()
