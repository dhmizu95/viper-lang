# Simple test for @lru_cache with non-recursive function
@lru_cache(maxsize=128)
def double(n):
    return n * 2

def main():
    print("double(5) =", double(5))
    print("double(10) =", double(10))
    print("double(5) again =", double(5))  # Should hit cache
    return 0

main()
