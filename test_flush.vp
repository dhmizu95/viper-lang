@lru_cache(maxsize=None)
def fib(n):
    if n <= 1:
        return n
    return fib(n - 1) + fib(n - 2)

def main():
    import sys
    print("fib(10) =", fib(10))
    sys.stdout.flush()
    print("Done!")
    sys.stdout.flush()
