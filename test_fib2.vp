@lru_cache(maxsize=None)
def fib(n):
    if n <= 1:
        return n
    return fib(n - 1) + fib(n - 2)

def main():
    print("fib(10) =", fib(10))
    print("fib(20) =", fib(20))
    print("fib(30) =", fib(30))
    print("Done!")
