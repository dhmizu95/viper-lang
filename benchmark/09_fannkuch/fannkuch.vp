# Benchmark 09: Fannkuch

def main():
    N = 6
    
    arr = [0] * N
    i = 0
    while i < N:
        arr[i] = i
        i = i + 1
    
    print("Permutations of " + str(N) + " elements")
    print("Max flips: " + str(N))
